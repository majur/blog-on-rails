<h1>List of Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to(user == current_user ? "#{user.name} (Current user)" : user.name, user_path(user)) %>
    </li>
  <% end %>
</ul>
