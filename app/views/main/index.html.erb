<div class="posts">
    <h2>Posts</h2>
    <% @posts.each do |post| %>
        <p>- <%= link_to post.title, post %> - <em>Published by <%= post.user.name %> on <%= post.created_at.strftime('%B %d, %Y') %></em></p>

    <% end %>
  </div>